@model IEnumerable<PrjFinanceiro.Models.Funcionario>

<div class="container mt-5">
    <h2 class="mb-4">Lista de Funcionarios - @ViewBag.nomesenaii</h2>

    <p>
        <a href="/Funcionario/Criar" class="btn btn-success">Cadastrar Novo Funcionario</a>
    </p>

    <table class="table table-bordered table-hover">
        <thead class="thead-dark">
            <tr>
                <th>Código</th>
                <th>Data de Nascimento</th>
                <th>Nome</th>
                <th>Cidade</th>
                <th>Estado (UF)</th>
                <th>CPF</th>
                <th>Telefone</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>                    
                    <td>@item.Codigo</td>
                    <td>@item.DataNascimento.ToString("dd/MM/yyyy")</td>
                    <td>@item.Nome</td>
                    <td>@item.Cidade</td>
                    <td>@item.EstadoUF</td>
                    <td>@item.CPF</td>
                    <td>@item.Telefone</td>
                    <td>
                        <a href="/Funcionario/Editar/@item.Codigo" class="btn btn-sm btn-primary">Editar</a>
                        <a href="/Funcionario/Excluir/@item.Codigo" class="btn btn-sm btn-danger">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>